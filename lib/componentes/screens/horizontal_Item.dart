import 'package:flutter/material.dart';

class horizontalItem extends StatelessWidget {
  final Map paquetes;

  horizontalItem({this.paquetes});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
